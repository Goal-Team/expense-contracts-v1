/.phpunit.cache
/node_modules

/public/build
/public/hot
/build
/hot
/public/storage
/storage/*.key
/vendor

.env
.env.backup
.env.production
.phpunit.result.cache
error_log

docker-compose.override.yml

Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/.fleet
/.idea
/.vscode

!/public/assets
!/public/assets/*
/public/css
/public/js
app/Helpers/barryvdh/laravel-dompdf/CHANGELOG.md
app/Helpers/barryvdh/laravel-dompdf/composer.json
web.config
# Vite writes a throwaway vite.config.<ext>.timestamp-*.mjs next to the config
# while loading it. Ignore only those. A blanket `*.mjs` used to sit here and it
# is what kept the root vite config out of git entirely.
/vite.config.*.timestamp-*.mjs
/plans
database/phase1_migrations.sql
/bootstrap/cache
.claude/launch.json
