# Measurement report — contract detail page

One file for this effort. **No old-number column** — the row above holds the previous number, so
writing it twice adds nothing (the dev's call 2026-08-21, [CLAUDE.md](../../../CLAUDE.md)). Row 0 is
the baseline, so the first real row has something to sit under. Never start a second report file.

**How to fill a row.** Same page, same data, same session. Absolute milliseconds on this machine vary
about 3x between sessions, so a number taken hours after the row above it means nothing. Prefer query
counts; they do not drift.

**Every row records bytes too**, not only time: **document bytes** — what the server rendered;
**total transfer bytes** and **request count** — what the browser actually pulled down. They answer
different questions and must not be reported as if they were one number.

**Take every row with the debug bar OFF.** Set `DEBUGBAR_ENABLED=false` in [.env](../../../.env)
first. On the dashboard it inflated the document from 63,274 bytes to 359,490 — 5.7x — plus two extra
asset requests.

Dataset for every row unless the row says otherwise: the seeded local set — **3,018 contracts**,
6,940 party rows, 13,867 approval rows. Page under test:
`http://apollo.contracts.legality:8888/contracts/contracts/100479?tab=edit`.

## Results

| # | change | TTFB | queries | document bytes | transfer bytes | requests | measure | side effects / remarks |
|---|---|---|---|---|---|---|---|---|
| 0 | baseline | 4,208–4,589 ms warm (4 runs); server `total_ms` 4,137–4,774 over 21 runs | 253 | 326,254 | 326,854 warm / **2,979,504 cold** | 59–60 warm / 62 cold | `PerfTimingMiddleware` per-request record in `storage/logs/perf-2026-08-22.log`, plus the browser Navigation Timing API; debug bar off, opcache on | Warm full load 5,527–5,959 ms; cold full load 8,083 ms. **Almost all of it is one query.** DB time is 3,674–4,282 ms, which is 89–90% of the request. Three queries are 90% of that DB time: the `GROUP_CONCAT` child walk (`ContractController.php:784`) at 2,130–2,616 ms, `$contractsoldothers` (`:723`) at 969–1,045 ms, and the parent walk (`:748`) at 174–179 ms. Bootstrap 131–223 ms, routing 3–11 ms, route middleware 21–38 ms, blade render 31–110 ms over 21 views, send 1 ms. Peak memory 12–14 MB, 831 files included. **The document is not compressed** — `encodedBodySize` equals `decodedBodySize` at 326,254 both ways, on cold and warm runs alike, while 39 of the asset requests are compressed. IIS gzip never engages for the HTML. `SHOW GLOBAL STATUS LIKE 'Questions'` read 259 across the same load, 6 more than the middleware's 253; the extra 6 are the two `mysql` CLI calls that took the readings. Use 253. Times vary about 15% between runs today, not 3x, because one query dominates. Control contract `1?tab=edit`: TTFB 4,367 ms, document 321,453 bytes, **282 queries**. Same page on other tabs: `?tab=history` 61,452 bytes and 240 queries, no `?tab` 105,797 bytes and 243 queries. |
| 1 | ticket 02: seed all 60 columns the page reads | over 120 s, then HTTP 500 | — | 0, IIS error page | — | — | browser, `100479?tab=edit` and `1?tab=edit` | Not a regression the seed caused by being wrong — the seed made a page cost visible. Rows went from ~1.5 KB to 9,390 bytes, `contracts` from roughly buffer-pool size to 110 MB `DATA_LENGTH` against a 16 MB `innodb_buffer_pool_size`. The child-contract `GROUP_CONCAT` query (ContractController.php:780) scans the whole table once per row and now exceeds the IIS FastCGI request timeout. Same 3,018 `(id, parentcontract)` pairs in a two-column temp table: **3 s**. Pre-existing contract 1 fails the same way, so this is not about the seeded rows. |
| 2 | ticket 11 (part): covering index on `contracts(parentcontract, id)` | 4,422 ms | — | 326,554 | — | 58 | browser, `100479?tab=edit`, warm cache, debug bar off | **The page renders again.** From HTTP 500 to 4.4 s. Not the ticket 04 baseline: `ContractController.php` has uncommitted edits from the ticket 14 agent, and this was a warm run, so query count and transfer bytes are not taken here. Two things worth keeping: the document is **326 KB**, about 5x the dashboard's 63 KB, with the debug bar off. And the index took **474 s to build** on 3,018 rows - production needs a window for it, not a quiet deploy. |
| 3 | ticket 14: guard `$contractsoldothers`, delete the dead subseq query, one approvals query instead of two | — | 258 | — | — | — | `SHOW GLOBAL STATUS LIKE 'Questions'` around one warm browser load of `100479?tab=edit`, debug bar off, each reading taken twice | Three commits: `63a1db2`, `d1173aa`, `8858fce`. The dead query cost 2, not 1 - itself plus the `contractPartyList` eager load `Contract::$with` adds to every `Contract` query. The approvals dedupe also removes one full seven-column decrypt pass over every approval row, which the query count does not show. Chart View proved byte-identical: 115,939 bytes rendered before and after on contract 4, which has 21 approval rows. **258, not the 375 ticket 08 counted by reading** - the inventory counted query sites and loop shapes, this counts what the server ran on one contract. |
| 4 | ticket 16: no code change — reading of the Details tab, the tab that renders the Related Contracts region | 4,268–4,571 ms over 3 warm fetches; 4,444 ms Navigation Timing; server `total_ms` 4,248–4,773 over 6 runs | **362** | 239,091 | 239,991 warm | 61 | `PerfTimingMiddleware` record in `storage/logs/perf-2026-08-22.log`, plus Navigation Timing on a real logged-in load of `100479?tab=details`; debug bar off, warm | **The region is not dead. `?tab=details` renders it.** Row 0 measured `?tab=edit`, so this row measures a different tab, not a change — nothing was deleted and nothing was added. The Details tab runs **362 queries against the edit tab's 253**, because the region's blade lazy-loads `contractParent` for each of 58 rows (`select * from contracts where parentcontract = ? limit 1`, 58 times, 65 ms). Time is the same 4.2–4.6 s, because the three whole-table scans run on every tab. The two Related-Contracts tables render **empty**: no contract in the set has a parent, so the parent walk and the child walk cost 2,496 ms to produce nothing. Document is still uncompressed — `encodedBodySize` equals `decodedBodySize`. |
| 5 | ticket 18: run the three whole-table scans only on the tab that renders them (`?tab=edit`) | 455–457 ms warm (2 navigations); server `total_ms` 337–371 over 3 runs | **86** | 326,254 | 326,554 warm | 61 | `PerfTimingMiddleware` record in `storage/logs/perf-2026-08-22.log`, plus Navigation Timing on a real logged-in load of `100479?tab=edit`; debug bar off, warm | **The edit tab is about 10x faster and runs a third of the queries.** The three scans are gone from the perf record's `slowest` list, so they did not run — the child walk `GROUP_CONCAT`, `$contractsoldothers` and the parent walk. The query count falls from 258 to 86 because the four `availableContracts()` decoration loops over 58 related contracts sat inside the same guarded block. Document bytes do not move: the edit tab never rendered the region, so it never carried its bytes. Same result on control contract `1?tab=edit`: 343–371 ms, 87 queries. Every tab loads: `pre-approval`, `timeline`, `edit`, `flow`, `history`, `attachment`, `obligation`, `e-stamp` and no `?tab` all return 200 with no scans. `?tab=historical` still returns HTTP 500 on `Undefined array key "history"` — unchanged, and [ticket 03](../issues/03-find-remaining-breaks.md)'s. |
| 6 | ticket 18, same commit, the Details tab (`?tab=details`) | 4,285 ms warm; server `total_ms` 4,103–4,446 over 2 runs | 360 | 239,091 | 239,391 warm | 61 | same tools, `100479?tab=details` | **This row barely moves, and that is the point.** The Details tab still runs the three scans, because it is the tab that renders their results. The rendered HTML is **byte-identical**: 239,091 document bytes before and after, and the two documents match exactly when whitespace is ignored. `Related Contracts`, `accordionParentContract` and `Category Previous Contracts` are all still present. Queries fall 362 to 360 — the duplicate `ContractParties::select('*')->get()` went, and `BranchScope` adds a second query to every one of those, so one call site cost two. |
| 7 | ticket 20 (half 1): index `contracts(contract_type, department_id, catgoery_id)` | 3,295–3,433 ms over 3 warm fetches; server `total_ms` 3,295–3,433 over 3 runs | 360 | 239,091 | — | — | `PerfTimingMiddleware` record in `storage/logs/perf-2026-08-22.log` plus `fetch()` timing on the logged-in page, `100479?tab=details`, debug bar off, warm | **The `$contractsoldothers` query goes from 928–1,823 ms to under 5 ms.** It no longer reaches the perf record's ten-slowest list, whose smallest entry on these runs is 4.31–7.39 ms. `EXPLAIN` before: `type ALL`, `key NULL`, `rows 1509`. After: `type ref`, `key contracts_type_department_category_index`, `key_len 171`, `ref const,const,const`, `rows 1`. Column order is most selective first — `contract_type` about 41 rows per value, `department_id` 84, `catgoery_id` 1006 — although all three tests are equality, so any order serves this one query. **The index built in 208 ms** on 3,018 rows, against 474 s for `(parentcontract, id)`: it holds three narrow columns, not the whole row. `contract_type` and `catgoery_id` are `TEXT`, so the index needs a prefix length (20 characters) and the migration writes raw SQL — Blueprint cannot express a prefix. Query count does not move. Document identical, ignoring whitespace, on `100479?tab=details` and on `1?tab=details`. Every tab loads; `?tab=historical` still 500 on `Undefined array key "history"`. **The remaining time is the child walk** ([ticket 15](../issues/15-recursive-child-walk.md)), 1,977–3,377 ms, and it varies run to run more than anything else on this page. |
| 8 | ticket 20 (half 2): read seven columns for Category Previous Contracts, not `select *` | 2,997–3,576 ms over 5 warm fetches; server `total_ms` 2,972–3,567 over 5 runs | **359** | 239,091 | 239,391 warm | 62 | same tools, `100479?tab=details` | The table prints five cells and links the row, so it reads seven columns: `id`, `contract_name`, `signing_date`, `currency`, `currency_value`, `fixed_date`, `contract_end_date`. A `contracts` row is 9,390 bytes wide. **The time win is small because row 7 already removed the scan** — the query reads about one row either way. The query count falls by one, from `without('contractPartyList')`: `Contract::$with` eager-loads party rows on every `Contract` query and this table shows none. On control contract `1?tab=details` the count falls **415 to 413**, by two, because `$contractsoldothers` returns 11 rows there and 0 on 100479 — Laravel skips the eager load on an empty result. **`withoutGlobalScope('accessLevelSelect')` is what makes the narrowing real**: `Contract::boot()` adds a global `select('*')` scope that runs after the `select()` here. Proved with a temporary `Log::debug` of `toSql()`, which printed the seven column names. Rows unchanged: both documents identical ignoring whitespace, and `1?tab=details` renders the same 11 rows in that table. |
| 9 | **new data, no code change**: the seeder now links 684 of the 3,000 seeded contracts to a parent ([ticket 15](../issues/15-recursive-child-walk.md) step 0) | 3,436 ms Navigation Timing; server `total_ms` **3,235–7,109 ms** over 5 warm fetches | **369** | 239,091 | 239,991 warm | 61 | `PerfTimingMiddleware` record in `storage/logs/perf-2026-08-22.log` plus Navigation Timing, `100479?tab=details`, debug bar off, warm | **This row is not a change. It is the same page on different data, and it is the honest starting point for ticket 15.** Row 0 and every row above it were taken on data where `SUM(parentcontract <> 0)` was **0**, so both recursive walks returned nothing. The shapes seeded: 300 pairs, 100 chains three rows deep, 50 chains four rows deep, and two wide fan-outs of 12 and 20 children with one branch two rows deeper. 77% of rows stay a root. **No cycle exists**: every parent id is below its child id (0 rows fail `parentcontract >= id`), and a recursive walk from the 2,334 roots reaches all 3,018 rows exactly once at a maximum depth of 3. The child walk now costs **1,996–5,365 ms** and swings wider than before. Query count rises 359 to 369: the region's blade lazy-loads `select * from contracts where parentcontract = ? limit 1`, and rows that now have a parent add hits. Document bytes do not move, because 100479's only child (100480) is dropped by `availableContracts()` — the user cannot see its branch. Contracts that do show children: **101101** (fan-out) 3,427–4,747 ms, **619** queries, 313,381 characters; control **1** 3,375–3,510 ms, **426** queries, 261,223 characters. |
| 10 | ticket 15: one `WITH RECURSIVE` query in place of the `@pv` / `FIND_IN_SET` child walk | 1,237 ms Navigation Timing; server `total_ms` **1,198–2,207 ms** over 5 warm fetches | 369 | 239,091 | 239,391 warm | 61 | `PerfTimingMiddleware` record in `storage/logs/perf-2026-08-22.log` plus Navigation Timing, `100479?tab=details`, debug bar off, warm | **The child walk leaves the ten-slowest list altogether.** The slowest query on the page is now the parent walk at 222–255 ms, and the second is 20–99 ms. The walk itself was 1,996–5,365 ms on row 9. Query count does not move: it was one query and it is one query. The rewrite also drops the `foreach` over the ancestors, so a contract three deep ran three walks and now runs one. Rendered HTML **identical ignoring whitespace on all five contracts checked** — `100479`, `1`, `101101`, `100603`, `101143` — with the same document bytes on the first three. `100603` and `101143` grow by exactly 88 characters each, and every one of those characters is whitespace: the id list is now correct, so the `@foreach` runs one more time and the shared `$prevContracts` list skips the row because the Parent Contracts table above already printed it. Other contracts on the Details tab: **101101** (12-child fan-out) 1,883–2,053 ms, 619 queries; control **1** 1,543–1,784 ms, 426 queries; `100603` 1,378 ms, 381 queries; `101143` 1,609 ms, 400 queries. Every tab loads on both contracts with the same document size ticket 20 recorded. `?tab=historical` still returns 500 on `Undefined array key "history"` — [ticket 03](../issues/03-find-remaining-breaks.md)'s — and it is now 3,919–3,985 ms instead of 6,676, because it fell into the same branch and paid the same walk. Browser console holds the same nine entries. |
| 11 | ticket 21: one `WITH RECURSIVE` query in place of the `@idlist` / `FIND_IN_SET` parent walk, sharing the `ancestry` fragment with the child walk | 824 ms Navigation Timing; server `total_ms` **686–785 ms** over 3 warm fetches | 369 | 239,091 | 239,391 warm | 61 | `PerfTimingMiddleware` record in `storage/logs/perf-2026-08-22.log` plus Navigation Timing, `100479?tab=details`, debug bar off, warm | **Neither walk is in the ten-slowest list any more.** The parent walk was 197–225 ms and the slowest query on every Details-tab load; the slowest query is now 7–11 ms on `101101` and 6–8 ms on `100904`. The recursive query costs 8.85 ms when it appears at all. Query count does not move on any contract: one query became one query. The upward walk now lives in one method, `ancestryCte()`, and both walks read it — the child walk already needed it to find the top of the tree. **Id sets match on all 20 contracts checked**: 5 roots, 3 one deep, 5 two deep, 5 three deep, both fan-outs under `101101`, and contracts 1, 2, 3. The old query returned one extra row holding `0` for a root, which the caller bound into a `whereIn` no id could match. Rendered HTML **byte-identical** on `101101`, `1`, `101143` and `100904`. Other contracts, Details tab: **101101** (12-child fan-out) 1,121–1,294 ms, 619 queries; control **1** 842–1,021 ms, 426 queries; `101143` (three ancestors) 888–1,081 ms, 400 queries; `100904` (three ancestors) 780–808 ms, 379 queries. Every tab loads on `101101` and `1`; `?tab=historical` still returns 500 on `Undefined array key "history"` — [ticket 03](../issues/03-find-remaining-breaks.md)'s. `laravel.log` holds no other error across 22 loads. |
| 12 | ticket 03: `?tab=historical` renders instead of throwing | 917 ms Navigation Timing; server `total_ms` **760–777 ms** over 3 warm fetches | 369 | 239,132 | 239,732 warm | 62 | `PerfTimingMiddleware` record in `storage/logs/perf-2026-08-22.log` plus Navigation Timing, `100479?tab=historical`, debug bar off, warm | **This row is a break becoming a page, not a speed change.** The tab returned HTTP 500 on `Undefined array key "history"` on every row above. It now returns 200 and **costs what the Details tab costs** — row 11 reads 686–785 ms and 369 queries on the same contract — because `historical` has no body block of its own and falls into the Details branch, so it runs the same work. Nothing was made faster and nothing was made slower: the earlier 3,700–4,100 ms figure for this tab was measured before tickets 15 and 21 replaced the two family-tree walks, and the 500 came after that time was already spent. Document is 41 characters larger than `?tab=details` (239,132 against 239,091); the difference is the tab bar, which lists a different set of links on this branch. The other three fixes in this ticket move no number at all: they add a null check, a `is_string()` test and one moved `if`, each on a path that runs once per request. Every tab returns 200 on `100479`, `101101`, `101143` and `1` — 44 loads, no error in `laravel.log`, and the browser console holds the same nine entries ticket 21 recorded. |
| 13 | ticket 19: the attachment tab asks Google Drive for the same file once, not twice | 1,356 ms Navigation Timing; server `total_ms` **1,327–1,384 ms** over 3 warm fetches | **89** | 61,365 | 61,965 warm | 62 | `PerfTimingMiddleware` record in `storage/logs/perf-2026-08-22.log` plus Navigation Timing, `100479?tab=attachment`, debug bar off, warm | **The time was never in the database. 89 queries cost 154–176 ms; the blade render held 1,827–1,940 ms of a 2,171–2,428 ms request.** Temporary `Log::debug` timing named the two lines: `fileViewUrl()` cost 915–971 ms and `get_google_drive_doc_link()` cost 863–975 ms, and both end in `GoogleDriveController::changePermission()` with the same file id, the same logged-in email and the same `$onlyView` flag. Each call makes two outbound requests to Google — an OAuth token refresh at 494–582 ms, because the client never holds a token so `isAccessTokenExpired()` is always true, and a Drive `files.get` at 359–422 ms. Four round trips for one link. `fileViewUrl()`'s answer is read only by the `Local` branch of the blade, so it moves inside that branch and the Google path makes one round trip. Local storage is unchanged: there `fileViewUrl()` is a `Storage::exists()` on disk. Query count falls 91 to 89 because `fileViewUrl()` called `fileStorageType()` twice and each call is `select type from file_storage where id = ?`. **Output unchanged.** `100479` still prints the storage-mismatch notice, and control contract **4** — a real Google Drive file, contract in Signing — still renders its `docs.google.com` iframe and goes 2,428 ms and 94 queries to **1,365 ms and 92**. The seeded contracts hold a made-up Drive id (`SEEDPERF000000100479`), so `files.get` throws for them, but the cost is the same either way: contract 4's `files.get` succeeded in 422 ms. All 13 tab values on `100479` and on `1` return 200, `laravel.log` holds no error across the 26 loads, and the console holds the same entries as row 12 plus one 403 from the Google Docs iframe, which Drive returns to an unsigned-in embed and which row 12 never had because that row's tab shows no iframe. **The remaining 915 ms is one Google Drive round trip the page really needs**, and it belongs off the page load — see the ticket's Resolution. |
| 14 | ticket 22: the Google Drive access token is cached, so the page stops refreshing it on every load | 1,198 ms Navigation Timing; server `total_ms` **1,066–1,212 ms** over 5 warm fetches | **89** | 61,365 | 61,965 warm | 62 | `PerfTimingMiddleware` record in `storage/logs/perf-2026-08-22.log` plus Navigation Timing, `100479?tab=attachment`, debug bar off, warm | **The refresh count is the proof, not the time.** `changePermission()` built a fresh `Google_Client` and never set a stored token, so `isAccessTokenExpired()` was always true and every call paid an OAuth refresh. The token now lives in the cache. `laravel.log` across 12 Drive calls holds **one** real `cache miss, refresh runs` line — the first load after `cache:clear` — and **10** `cache hit, no refresh` lines; the twelfth miss is the forced one from the retry test below. The cache key is a `sha1` of the client id, the client secret, the token endpoint and the refresh token, so two Google accounts or two credential files cannot share an entry. The entry lives **3,479 seconds**, 120 short of the token's own 3,599, so no request picks up a token that dies mid-call. **The retry path was proved, not assumed:** a bogus access token was written into the cache by hand, and the load logged `Google Drive rejected the access token`, code 401, refreshed once, retried once, and rendered contract 4's `docs.google.com` iframe as before. A 404 for a missing file does not touch the cache, so the seeded contracts' made-up Drive ids still cost one round trip and no refresh. **The win is smaller than row 13 predicted.** Row 13 measured the refresh at 494–582 ms; today the same refresh costs about **230 ms**, read from the one miss load at 1,392 ms against the hit loads at 1,066–1,212 ms in the same session. The refresh got cheaper between sessions, not the cache less useful — the round trip to `oauth2.googleapis.com` is what it is on the day. Control contract **4**, a real Drive file, goes 1,365 ms and 92 queries to **1,194–1,256 ms and 92**, so both the failure path and the success path improve by the same amount, as row 13 said they would. **Output unchanged:** document, transfer and request count are byte-for-byte row 13's numbers, and all 13 tab values on `100479` and on `1` return 200 with the same 26 character counts ticket 03 recorded. `laravel.log` holds no error, and the console holds the same seven entries as row 13. |
| 15 | ticket 17: gzip the HTML document in PHP, because IIS cannot | 471 ms cold / 571 ms warm Navigation Timing; server `total_ms` **366–476 ms** over 6 warm fetches | 86 | **35,432** encoded / 326,254 decoded | **35,732 warm / 2,688,682 cold** | 62 | `PerfTimingMiddleware` record in `storage/logs/perf-2026-08-22.log`, the `CompressResponse` `Log::debug` line, plus Navigation Timing and `fetch()` on the logged-in page, `100479?tab=edit`, debug bar off | **The document is compressed at last: 326,254 bytes to 35,432, 9.2x.** Both proofs together — the response header reads `content-encoding: gzip` with `content-length: 35432` and `vary: Accept-Encoding`, and `encodedBodySize` reads 35,432 against `decodedBodySize` 326,254. **The fix is PHP, not IIS, and that is not a choice.** IIS static compression and IIS dynamic compression are two different modules. The static one is installed and compresses the 39 assets; the dynamic one is **not installed on this server** — `compdyn.dll` is absent from `C:\WINDOWS\System32\inetsrv`, `applicationHost.config` holds no `<dynamicTypes>` list and no `dynamicCompressionLevel`, and `grep` finds no `DynamicCompression` string anywhere in that file. So no line in any `web.config` can switch it on; it needs a Windows role feature and an administrator. `web.config` was **not changed** by this row. `serverRuntime frequentHitThreshold="1"` from the dashboard effort **is** applied and live, and `frequentHitThreshold` turns out not to delay dynamic compression at all here, because there is no dynamic compression to delay — the very first request for the document comes back gzipped. **The CPU cost is 6–9 ms and the perf record isolates it cleanly.** With the middleware off, `send_terminate_ms` on the edit tab reads 0.70–0.87 ms over 6 fetches; with it on, the same phase reads 7.31–9.38 ms. The `Log::debug` line agrees from the other side: over 65 compressed responses, `gzencode` at level 6 costs a median of 3.29 ms, 6.35 ms median on an edit-size document, 8.36 ms worst, and saved 9,715,902 bytes for 234 ms of CPU in total. **Level 6 is the knee of the curve**, measured on the real document: level 1 gives 42,790 bytes in 2.0 ms, level 6 gives 35,432 in 4.6 ms, level 9 gives 34,648 in 17.2 ms — so level 9 pays 12 ms more for 784 bytes. **TTFB medians move by more than the compression costs, and the ranges overlap**, so the 6–9 ms is the number to trust: edit tab off 360–469 ms median 404, on 374–481 ms median 435; details tab off 760–847 ms median 787, on 760–946 ms median 869. Six fetches each, same session, middleware switched off and back on. **The whole-page number matters more than the document number.** Cold goes 2,979,504 to **2,688,682 bytes**, 290,822 off, 9.8%. Warm goes 326,854 to **35,732 bytes**, 89% off, because on a repeat visit every asset comes from cache and the document *is* the page. Request count unchanged at 62. **Every tab still renders the same bytes.** All 13 tab values on `100479` and on `1` — 26 loads — return 200 and compress, at 4.8x to 11.9x. Decoded size proved unchanged the strict way, not against an older record: the same 26 loads were taken with the middleware commented out of `Kernel.php` and back in, and all 26 decoded sizes match **exactly, delta 0**, with `encodedBodySize` equal to `decodedBodySize` on all 26 off-runs. The decoded edit-tab body is byte-identical to the pre-change body once the CSRF token is normalised. `?attachment=1` returns 200 at 105,779 characters, exactly ticket 03's figure, and `?tab=historical&history=999999` returns 200. `laravel.log` holds no error across the 55 loads and one deliberate `history=999999` warning; the console holds the same entries as row 14, including the pre-existing 403 on the external `s3-us-west-2.amazonaws.com` copy of `jSignature.min.js`. **The middleware refuses to touch four things on purpose:** `StreamedResponse` and `BinaryFileResponse`, so a contract attachment download is never read into memory; any response that already carries a `Content-Encoding`; bodies under 1 KB, where the gzip header costs more than it saves; and content types that are already compressed, so the 820 KB of woff2 fonts are left alone as they should be. |
| 16 | ticket 09: the Parent Contracts list reads the ancestor walk as a subquery instead of binding its ids | server `total_ms` **688–834 ms** over 3 warm fetches | **368** | 239,091 | — | — | `PerfTimingMiddleware` record in `storage/logs/perf-2026-08-22.log` plus `fetch()` on the logged-in page, `100479?tab=details`, debug bar off, warm; before and after taken in one session with `git stash` between | **One query less on every contract, and the point is not the query count.** `Contract::select('*')->whereIn('id', $parentContractArr)` bound the ancestor ids one at a time, and on this stack a `whereIn` with 1,000 or more bound values returns **zero rows with no error**, so the Parent Contracts table would go blank on a deep tree and look like missing data. `ancestorContractIds()` now returns the **query**, not an array, and `whereIn` reads it as a subquery: one binding, the contract id, and no id crosses the wire. Two queries became one on every contract: **369 to 368** on `100479`, 426 to 425 on control `1`, 400 to 399 on `101143` (three ancestors), 619 to 618 on `101101` (20-child fan-out). **This is CLAUDE.md's level 2, and level 1 cannot reach it** — a relationship needs a fixed number of joins and an ancestor chain has no fixed depth. The recursion stays raw, which CLAUDE.md already names as the one real exception, and it is the same `ancestryCte()` fragment tickets 15 and 21 built, so nothing new is raw and nothing is copied. **MariaDB accepts a `WITH` clause inside a derived table** — that is what lets `whereIn` hold the walk. `withoutGlobalScope('accessLevelSelect')` was **not** needed, unlike ticket 20: the `select('*')` trap only fires when the subquery is a `Contract` query, and this one is a plain `DB::query()`. No `distinct()` needed either — one `parentcontract` per contract, so the chain cannot fork. **Honest note: the merged query is 2–5 ms slower than the two it replaced.** Six `tinker` runs of each shape, first dropped: `101143` 5.54–9.00 ms old against 7.96–9.47 ms new, `100904` 3.08–11.67 against 7.72–19.89, `101102` 2.85–8.63 against 8.20–13.99, `100479` 1.94–7.08 against 5.71–9.94. It now shows in the ten-slowest list at 6.51–13.10 ms where neither of the old two appeared, and page `total_ms` does not move, so it sits inside the run-to-run swing. **The win is that the table cannot silently go blank**, plus one round trip less. `EXPLAIN` proves it reads no more than before: every touch of `contracts` is `eq_ref` or `const` on `PRIMARY`, no scan at any depth. **`orderBy('id')` is the old order written down, not new behaviour.** The bound `whereIn` returned rows in primary-key order; the subquery makes MariaDB read the walk first, which returns the nearest parent first, so `101143` came back `101142, 101102, 101101`. The table has always printed the lowest id first, so the sort keeps the page as it was. **Columns stay at `select('*')` on purpose** — `availableContracts()` runs over these rows, reads at least ten columns and **branches on `isset()`**, so a column left out changes the rows and the query count with no error; and the blade loops `contractPartyList` for the Unlink button, so the eager load stays too. Ticket 20's narrow select was safe only because that query has no `availableContracts()` pass. **Id sets match on 22 of 22 contracts**, ids and order both — 8 roots, 5 one deep, 4 two deep, 5 three deep — compared as the row lists the page loops, not as the ids the walk found. **Output unchanged:** 30 documents, all 13 tab values on `100479` and `1` plus four Details-tab contracts, every one the same length and the same hash as before, whitespace ignored, and the Parent Contracts table printing the same ids in the same order. All 13 tabs return 200. `laravel.log` holds no error and no warning across the run. **One debug line changed name:** `walked up the contract family tree` is gone, because the ids no longer come back to PHP and nothing can count them; `read the parent contracts` logs the row count the page shows, which is after the visibility scope rather than before it. |
| 17 | ticket 03: the timelineedit missing-fields table reads `$reqfieldsText` | — | — | 82,044 raw / 59,165 whitespace-normalised on `100479?tab=timelineedit` | — | — | `fetch()` on the logged-in page, 30 documents across 13 tab values on `100479` and `1`, plus `16` and `101143`, debug bar off | **No number moves, and this row says so rather than inventing one.** Ticket 03 found `?tab=timelineedit` throwing HTTP 500 on `Undefined variable $reqfields` and guarded the loop, so the tab rendered an empty table. The dev confirmed 2026-08-22 that the variable was renamed to **`$reqfieldsText`**, so the loop reads that map now and the table fills. It is a `display:none` table listing the contract fields that are missing, and `contract.js` disables the popup's Send button when it has rows, so the rows matter even though nobody sees them. **The shape fits the loop:** `$reqfieldsText` is `key => label`, the keys are `contracts` columns and the labels are plain strings, so `@empty($contract->$key)` works and no row prints a blank or an array-to-string notice. `100479` and `101143` print `Termination - Date` and `Signing Date`; `1` prints those two plus `Relationship with Apollo Group`. The `?? []` guard stays: the variable is always set today so it costs nothing, and it stops the next rename taking the page down. **Only the four `timelineedit` documents change**, by 618–858 raw characters each; the other 26 are identical to the character, whitespace ignored. All 13 tab values return 200 on both contracts. `laravel.log` holds no error or warning across the 30 loads, and the console holds the same two known entries — a 403 for an asset and the Tagify warning. **One key shape does not fit and it is left alone.** The controller adds a row to `$reqfieldsText` for every **required custom field**, keyed by `custom_field_id`, and those values live in `custom_field_data`, not on the `contracts` row — so `$contract->$key` is `null` and the field always prints "Missing". Contract **16** proves it: custom field 57 holds `Related Party` and the table still says `Relationship with Apollo Group | Missing`. `$reqfieldsVals[$key]` holds the real value, and `contractApprovalsView.blade.php` already pairs those two arrays that way. It is a wrong result, not a break, and it costs nothing, so the performance rule leaves it; the dev decides which array the test should read. | **Reverted 2026-08-22 by commit `0e7e045`** — the wiring switches on a hidden gate in `contract.js` that disables the Send buttons, so the table is empty again on `main`. The row stays as the record of what was tried.
| 18 | ticket 11: the last two indexes worth adding — `contracts_history(id, created_at)` and `custom_field_data(custom_field_group_id, custom_field_id, custom_field_group(20), id)` | server `db.total_ms` **434–680 ms** on `?tab=details`, **132–143 ms** on `?tab=edit`, 3 warm fetches each | **368** details / **96** edit | 239,629 details / 326,784 edit | — | — | `PerfTimingMiddleware` record in `storage/logs/perf-2026-08-22.log`, `fetch()` on the logged-in page, `100479`, debug bar off, warm; before and after in one session | **Neither index moves a number, and this row says so instead of inventing one.** Same 368 and 96 queries as before, and the millisecond spread overlaps the before spread (details was 443–502 ms, 806–996 ms wall). **They are still applied, and `EXPLAIN` proves they work**: both queries go from a full scan to `type ref, rows 1`. The reason no time appears is the row counts on the seeded set — `contracts_history` holds **17** rows and `custom_field_data` holds **6**, so a scan of either is free. Both tables grow with use on a client database: `contracts_history` gains a row on every save of any contract, and `custom_field_data` a row per custom field per contract. Build times 307 ms and 221 ms, so no maintenance window. **Two indexes ticket 11 named were dropped, not added.** `contract_party_data(contract_party_exe_id)` cannot help: the column holds **one** distinct value across 6,940 rows (3,018 rows hold `1`, the rest `NULL`), so the optimiser scans whatever we build, and the query is a ticket 09 site anyway. `user_action_log(group_id)` waits on ticket 12, which may delete the query it serves; the table holds 49 rows. |
| 19 | ticket 13 step 1: `admin_setting()` reads the row once per request, not once per `Contract` query | server `total_ms` **766–1,017 ms** details / **532–538 ms** edit, 5 warm fetches | **305** details (was 368) / **93** edit (was 96) | 239,629 details / 326,784 edit, both byte-identical to before | — | — | `PerfTimingMiddleware` record in `storage/logs/perf-2026-08-22.log`, `fetch()` on the logged-in page, `100479`, debug bar off, warm; before and after in one session | **63 queries off the Details tab for a five-line change.** `ContractRoledBasedScope` asks `admin_setting('enable_role_based_data')` once for every `Contract` query, so one settings row came back over the wire **64 times** in one page load. `AdminSettings::getValue()` now holds what it read in a static array for the life of the request. **No time is claimed**: 63 of those queries cost 65 ms in total and the wall-clock spread overlaps the spread before the change. The count is the honest number. A null value is a real answer, so the cache tests `array_key_exists`, not `isset`; `setValue()` and the model's `saved`/`deleted` events drop the entry, so a write in the same request cannot be served a stale value. |
| 20 | ticket 13 step 2: `availableContracts()` stops looking up a category and a contract type per row, and the Link-button check stops lazy-loading a child per row | server `total_ms` **438 ms** `100479?tab=details`, 394 ms on `1`, 514 ms on `101101`, 439 ms on `16` | **134** on `100479` (was 305), **135** on `1` (was 349), **137** on `101101` (was 502), **130** on `16` (unchanged) | 239,629 / 261,766 / 313,934 / 186,031 - all four byte-identical to before | — | — | `PerfTimingMiddleware` record in `storage/logs/perf-2026-08-22.log`, `fetch()` on the logged-in page, **32 documents - 4 contracts x 8 tab values**, debug bar off, warm; before and after taken in one session with `git stash`, and every one of the 32 hashes matches | **The query count stops growing with the family tree.** That was the whole complaint in ticket 13: 305 / 349 / 502 across three contracts became **134 / 135 / 137**. Three N+1 loops went: `ContractCategories::find()` per contract became one `pluck()`, held in a static for the four calls this page makes; `$contract->contractTypeData` became `loadMissing()` on the collection; and `$contractsoldother->contractParent`, read once per row in the Link-button check at `viewDetailContract.blade.php:2431`, became `with('contractParent')` on the query that builds the list. **The eager loads are skipped on a one-row collection**, because a collection of one costs the same query either way - without that guard contract `16`, which has few related contracts, went **up** by 3. Contract `16` is the control: it does not improve and it must not get worse. **No time is claimed**; the wall-clock spreads overlap and the removed queries were small. |
| 21 | ticket 13 step 3: `Helpers::getEntityBranches()` works the branch list out once per request, not once per scoped query | server `total_ms` **484 ms** `100479?tab=details`, 323 ms on `1`, 435 ms on `101101`, 298 ms on `16` | **104** on `100479` (was 134), **105** on `1`, **107** on `101101`, **100** on `16`; **81** on `100479?tab=edit` (was 93) | 239,629 / 261,766 / 313,934 / 186,031 - byte-identical again | — | — | `PerfTimingMiddleware` record in `storage/logs/perf-2026-08-22.log`, `fetch()` on the logged-in page, **the same 32 documents - 4 contracts x 8 tab values**, debug bar off, warm | **30 queries off every tab, not only Details.** `BranchScope`, `DepartmentScope` and `UserBranchScope` each call `getEntityBranches()` every time they build a query, and each call re-reads the `UserCredential` row for the auth token, then the `ContractUsers` row for that username, then walks the branch hierarchy - 41 identical queries in one load, two of them decrypting in the `WHERE`. The session cannot change while one request runs, so the answer cannot either. The body moved to `resolveEntityBranches()` and the public method is now the cache around it, so every return path is untouched. The key holds the two arguments plus the session token, entity and role. |
| 22 | ticket 13 step 4: the four access lists and the storage-type row are read once per request | server `total_ms` **323 ms** `100479?tab=details`, 475 ms on `1`, 415 ms on `101101`, 322 ms on `16` | **87** on `100479` (was 104), **88** on `1`, **90** on `101101`, **84** on `16`; **73** on `100479?tab=edit` (was 81), **62** on `?tab=attachment` (was 74) | 239,629 / 261,766 / 313,934 / 186,031 - byte-identical | — | — | `PerfTimingMiddleware` record in `storage/logs/perf-2026-08-22.log`, `fetch()` on the logged-in page, the same 32 documents, debug bar off, warm | Two more request-lifetime lists, same idea as rows 19 and 21. `availableContracts()` read the branch list, the business-function list, the entity list and the category list at the top of every call, and this page calls it four times - 12 queries for four answers that cannot change between the calls. And `fileStorageType()` read its one row every time it was asked; `fileStorageTypeController()` alone asks three times, and the page asked nine. **This one helps every tab**, including `?tab=attachment`, which does not run `availableContracts()` at all. |
| 23 | ticket 13 step 5: `Helpers::userInfo()` reads the logged-in user's row once per request | server `total_ms` **370 ms** `100479?tab=details`, 369 ms on `1`, 430 ms on `101101`, 314 ms on `16` | **82** on `100479` (was 87), **83** on `1`, **85** on `101101`, **79** on `16`; **68** on `?tab=edit` (was 73), **56** on `?tab=attachment` (was 62) | 239,629 / 261,766 / 313,934 / 186,031 - byte-identical | — | — | `PerfTimingMiddleware` record in `storage/logs/perf-2026-08-22.log`, `fetch()` on the logged-in page, the same 32 documents, debug bar off, warm | **Five queries, but the most expensive shape on the page.** 76 call sites ask for the logged-in user's row, and the query decrypts `UserName` inside the `WHERE`, so MariaDB reads and decrypts all 1,605 user rows every time it runs. The six calls on the Details tab cost 19.4 ms of the 106 ms the whole page then spent in the database. Same shape as rows 19, 21 and 22: the body moved to `resolveUserInfo()` and the public method is the cache around it, keyed on the three session values the answer depends on. **Nothing in the repo writes through the result** - checked for `userInfo()->x =`, `save()`, `update()` and `fill()` - so one model shared by every caller is safe. |
| 24 | ticket 12: the dead `SHOW TABLES` check leaves, and the auth-token user row is read once | server `total_ms` **367 ms** `100479?tab=details`, 297 ms on `1`, 487 ms on `101101`, 351 ms on `16` | **78** on `100479` (was 82), **79** on `1`, **81** on `101101`, **75** on `16`; **64** on `?tab=edit` (was 68), **52** on `?tab=attachment` (was 56) | 239,076 / 261,223 / 313,381 / 185,486 | — | — | `PerfTimingMiddleware` record in `storage/logs/perf-2026-08-22.log`, `fetch()` on the logged-in page, the same 32 documents, debug bar off, warm; before and after taken in one session with `git stash`, all 32 hashes equal | **`checkTablesConfiguration()` can never fail.** Its required-table list is an empty array, so every loop in it is empty and it always returns `true` - after running `SHOW TABLES` over the whole schema. `ContractSessionMiddleware` calls it twice per request, and at 9.1 ms it was the slowest single query left on the page. It now returns before the query, and the check below is left intact for the day the list is filled. The other half: `ContractSessionMiddleware` and `Helpers::getEntityBranches()` read the same `UserCredential` row with the same four decrypted columns, and `getEntityBranches()` is called with two different argument pairs, so the row was read three times. One shared `Helpers::authTokenUser()` now reads it once. **Note on the document bytes in this row.** They are 553 lower than row 23 on every tab, and no change of ours did it: the 60-minute `historical` cookie expired mid-session, so the Historical nav item stopped being drawn. That is the same behaviour the map records under the cookie decision, seen by accident. The before-and-after hashes for this row were taken after the expiry, on both sides, and all 32 match. |
| 25 | ticket 12: the six results nothing reads leave the view | server `total_ms` **306 ms** `100479?tab=details`, 403 ms on `1`, 300 ms on `101101`, 227 ms on `16`; database **93.6 ms** | **76** on `100479` (was 78), **77** on `1`, **79** on `101101`, **73** on `16`; **62** on `?tab=edit` (was 64), **50** on `?tab=attachment` (was 52) | 239,076 / 261,223 / 313,381 / 185,486 - byte-identical | — | — | `PerfTimingMiddleware` record in `storage/logs/perf-2026-08-22.log`, `fetch()` on the logged-in page, the same 32 documents, debug bar off, warm | Six names went out of the view payload: `$categorys`, `$signedHistory`, `$currentApproval`, `$isCurrentApprover`, `$attachmentUrl` and `$waitingGateGroupIds`. **Only two of them cost a query** - the `Category` read and `getSignedHistory()`, which reads `user_action_log` with no index - and that is the whole -2. The rest were PHP work and payload. **Every one was checked before deleting**: all blades in every module, all PHP, all JS, with backup files (`-bcck`, `-au11`, `-bc`) and the compiled cache under `storage/framework/views` ruled out as callers. Four of the six are read by blades that other controllers render, and each of those builds its own copy. `getSignedHistory()` itself stays - it has other callers. **One index question is answered by this row:** ticket 11 held `user_action_log(group_id)` because ticket 12 might delete the query it served. It did. |
| 26 | ticket 12 item 2: three repeated call sites read their answer once | server `total_ms` 269-820 ms across the four contracts on `?tab=details` - **too noisy to read**, another session was using the app | **72** on `100479` (was 76), **73** on `1`, **73** on `101101`, **70** on `16`; **58** on `?tab=edit` (was 62), **48** on `?tab=attachment` (was 50) | 239,076 / 261,223 / 313,381 / 185,486 | — | — | `PerfTimingMiddleware` record in `storage/logs/perf-2026-08-24.log`, `fetch()` on the logged-in page, the same 32 documents, debug bar off, warm; before and after taken with `git stash`, and the Details document compared character by character both ways | Found by tracing every query to its call site, not by reading: a temporary `DB::listen` wrote the SQL and four stack frames per query, and the trace was removed again. Three sites repeated: `get_country()` ran three times for the same country, `Helpers::getEntityBranches()` read the same `AddUsers` row for each of its two argument pairs - and that query decrypts `UserName` in the `WHERE` - and the party loop read the entity name once per party row. All three now read once. **Read the milliseconds in this row as nothing.** The clock on this machine moved to 2026-08-24 mid-session and another session was loading contracts at the same time, so the wall-clock spread is wider than the change. The query count is the number here. |
| 27 | ticket 12 item 2: the subject contract is read once, not twice | — (the machine is shared with another session; see row 26) | **70** on `100479` (was 72), **71** on `1`, **71** on `101101`, **68** on `16`; **56** on `?tab=edit` (was 58), **46** on `?tab=attachment` (was 48) | 239,076 / 261,223 / 313,381 / 185,486 - byte-identical | — | — | `PerfTimingMiddleware` record in `storage/logs/perf-2026-08-24.log`, `fetch()` on the logged-in page, the same 32 documents, debug bar off, warm | `viewContract` read the contract row at the top and read it again 200 lines later, and each read dragged the `Contract::$with` party rows behind it - **four queries for one row**. It could not simply reuse the first one, because `availableContracts()` writes decrypted names, formatted dates and label text back onto the model it is given. So the method now takes an untouched **clone** before that pass and the live-contract branch reads the clone. `clone` copies the attribute array by value, so the decrypt pass cannot reach it. The second read had no `where('status', 1)` and the first does; that changes nothing, because an empty first result redirects out of the method long before this line. |
| 28 | environment, not code: MariaDB buffer pool 16M → 512M (live `SET GLOBAL` + `my.ini`), and `composer dump-autoload -o` (77,881 classes in the classmap, was 1,367) | dashboard `/` warm: server `total_ms` **1,275 ms**, `db.total_ms` **481 ms** (was 1,027–1,222 ms on 2026-08-24), boot 602 ms | **13** on `/` (unchanged by this row) | — | — | — | `PerfTimingMiddleware` record in `storage/logs/perf-2026-08-27.log`, dashboard loaded twice logged-in through the CDP browser | **Every millisecond in rows above was measured through a 16 MB buffer pool** — the XAMPP template default, one-seventh of the 110 MB `contracts` table — so cold runs re-read the table from disk and warm runs drifted 3x. From this row on the pool holds the whole database. Numbers before and after this row do not compare. `my.ini` backup: `C:\xampp\mysql\bin\my.ini.bak-2026-08-27`. Autoloader backup: session scratchpad `autoload-backup\`. `composer.json` still says `optimize-autoloader: false`, so the next `composer install` or plain `dump-autoload` silently regenerates the slow autoloader — flip the flag to keep it. Found while testing: `route:list` throws on `App\Http\Controllers\layouts\NavbarFull` — dead template routes at `routes/web.php:173`, the controller file does not exist; pre-existing, not touched. |
| 29 | the icon stylesheets say what encoding they are | — | — | — | — | — | `curl -D-` on two stylesheets, plus a cache-ignoring reload of the dashboard and the contract detail page in the logged-in browser | **Not a speed change - a break, and the second report of it.** Every icon on the page turned into mojibake: the sidebar read `ï€—Dashboard` instead of drawing an icon. **Cause:** the Vite icon stylesheets carry the glyph as **raw UTF-8 bytes**, not a `015` escape - `tabler-icons-XUw5yIc1.css` holds `content:"<EF 95 94>"` - and IIS mapped `.css` to `text/css` with **no charset** (`applicationHost.config` line 497). A stylesheet with no charset of its own borrows the encoding of the page that links it, and when that does not happen the browser reads those bytes as Latin-1. **Fix:** one `<location>` block in `web.config` maps `.css` to `text/css; charset=utf-8`. Both stylesheets now answer with the charset, the `immutable` cache header on the build assets still arrives, and both pages render every icon after a cache-ignoring reload. All 108 stylesheets in the repo were read as UTF-8 first and all 108 decode cleanly, so the declaration cannot mis-read any of them. |
